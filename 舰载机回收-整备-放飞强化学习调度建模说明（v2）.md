# 舰载机回收-整备-放飞强化学习调度建模说明（v2）

## 1. 建模背景

本项目面向舰载机在飞行甲板上的多波次回收、整备与再次放飞过程，构建一个事件触发的动态强化学习调度环境。

当前模型已经从早期的“单轮全部放飞完成”问题，调整为“固定仿真时长内的多波次 A/B 组轮换出动问题”。

基本循环为：

```text
放飞 -> 空中 -> 回收 -> 停放 -> 加油/挂弹 -> 下一次放飞
```

其中，A/B 两组飞机交替放飞和回收：

| 波次编号 | 放飞组 | 回收组 |
|---:|---|---|
| 0 | A | 无 |
| 1 | B | A |
| 2 | A | B |
| 3 | B | A |
| ... | A/B 交替 | B/A 交替 |

若某架飞机未能在本组当前放飞窗口内完成整备并放飞，则记为一次 `missed_sortie`，等待下一次本组放飞波次。

## 2. 问题目标

早期单轮模型的目标是最小化最后一架飞机完成放飞的时间：

```text
min C_max
```

当前环境采用固定时长多波次仿真，因此优化目标应改为：

```text
max total_sorties_completed
min total_missed_sorties
```

可写成加权目标：

$$
\max J =
\mathbb{E}\left[
N_{\text{sortie}}
- \lambda N_{\text{miss}}
\right]
$$

其中：

- $$N_{\text{sortie}}$$：仿真期内完成放飞的总架次；
- $$N_{\text{miss}}$$：仿真期内累计错过波次次数；
- $$\lambda > 0$$：missed sortie 的惩罚权重。

当前实验评估主要使用：

```text
total_sorties_completed
total_missed_sorties
total_reward
```

其中 `total_reward` 是训练和调试辅助指标，不完全等价于最终评估目标。

## 3. 强化学习建模

当前环境采用事件触发的分层动作建模：

- 高层动作选择任务类型；
- 低层动作选择具体飞机；
- 环境根据资源状态、事件队列和波次状态推进；
- 当无可执行动作时，时间自动推进到最近事件。

完整动作形式为：

```python
action = {
    "high_level": int,
    "aircraft_id": int,
}
```

高层动作编码为：

| 编号 | 动作 | 含义 |
|---:|---|---|
| 0 | R | 回收 |
| 1 | F | 加油 |
| 2 | M | 挂弹 |
| 3 | L | 放飞 |

## 3.1 State

在第 $$e$$ 个事件触发决策时刻 $$\tau_e$$，系统状态定义为：

$$
s_e =
\left(
s_e^{air},
s_e^{res},
s_e^{wave}
\right)
$$

其中：

- $$s_e^{air}$$：飞机层状态；
- $$s_e^{res}$$：资源层状态；
- $$s_e^{wave}$$：波次层状态。

### 3.1.1 飞机层状态

对每架飞机 $$i$$，定义状态向量：

$$
x_{i,e} =
\left(
g_i,\,
z_i,\,
u_i,\,
h_i,\,
c_i,\,
m_i,\,
y_i^{(r)},\,
y_i^{(f)},\,
y_i^{(m)},\,
v_i^{(m)},\,
y_i^{(l)},\,
q_i,\,
\delta_i^{(f)},\,
\delta_i^{(m)},\,
w_i^{(f)},\,
w_i^{(m)},\,
w_i^{(l)}
\right)
$$

变量含义如下：

| 变量 | 含义 |
|---|---|
| $$g_i$$ | 飞机所属组，A 组记为 0，B 组记为 1 |
| $$z_i$$ | 停机位编号，空中或无停机位时为 -1 |
| $$u_i$$ | 停放状态 |
| $$h_i$$ | 是否处于空中 |
| $$c_i$$ | 是否等待回收 |
| $$m_i$$ | 已错过波次数 |
| $$y_i^{(r)}$$ | 回收状态 |
| $$y_i^{(f)}$$ | 加油状态 |
| $$y_i^{(m)}$$ | 挂弹状态 |
| $$v_i^{(m)}$$ | 挂弹阶段 |
| $$y_i^{(l)}$$ | 放飞状态 |
| $$q_i$$ | 本轮所需挂弹数量 |
| $$\delta_i^{(f)}$$ | 加油剩余时间 |
| $$\delta_i^{(m)}$$ | 挂弹剩余时间 |
| $$w_i^{(f)}$$ | 加油等待时间 |
| $$w_i^{(m)}$$ | 挂弹等待时间 |
| $$w_i^{(l)}$$ | 放飞等待时间 |

状态编码如下。

回收状态：

$$
y_i^{(r)} =
\begin{cases}
0, & \text{未回收或等待回收} \\
1, & \text{正在回收} \\
2, & \text{已完成回收}
\end{cases}
$$

加油状态：

$$
y_i^{(f)} =
\begin{cases}
0, & \text{未加油} \\
1, & \text{正在加油} \\
2, & \text{已完成加油}
\end{cases}
$$

挂弹状态：

$$
y_i^{(m)} =
\begin{cases}
0, & \text{未挂弹} \\
1, & \text{正在挂弹流程中} \\
2, & \text{已完成挂弹}
\end{cases}
$$

挂弹阶段：

$$
v_i^{(m)} =
\begin{cases}
0, & \text{未开始} \\
1, & \text{弹药提取与下层升降机运输} \\
2, & \text{弹药到达装配区，等待甲板挂弹资源} \\
3, & \text{上层升降机、甲板转运与逐弹挂载} \\
4, & \text{挂弹完成}
\end{cases}
$$

放飞状态：

$$
y_i^{(l)} =
\begin{cases}
0, & \text{未具备放飞条件} \\
1, & \text{等待放飞} \\
2, & \text{正在放飞} \\
3, & \text{已完成放飞并进入空中状态}
\end{cases}
$$

本轮所需挂弹数量在飞机回收完成时采样：

$$
q_i \in \{1,2,3,4\}
$$

概率分布为：

$$
P(q_i=1)=0.3,\quad
P(q_i=2)=0.4,\quad
P(q_i=3)=0.2,\quad
P(q_i=4)=0.1
$$

飞机刚放飞后，尚未回收前：

$$
q_i = 0
$$

### 3.1.2 资源层状态

资源层状态定义为：

$$
s_e^{res} =
\left(
R_e,\,
F_e,\,
A_e,\,
D_e,\,
U_e,\,
W_e,\,
P_e,\,
L_e,\,
S_e
\right)
$$

其中：

| 变量 | 含义 |
|---|---|
| $$R_e$$ | 空闲回收通道数量 |
| $$F_e$$ | 空闲加油服务能力数量 |
| $$A_e$$ | 空闲弹药转运车数量 |
| $$D_e$$ | 空闲下层武器升降机数量 |
| $$U_e$$ | 空闲上层武器升降机数量 |
| $$W_e$$ | 空闲挂弹车数量 |
| $$P_e$$ | 空闲保障人员数量 |
| $$L_e$$ | 空闲放飞通道数量 |
| $$S_e$$ | 空闲停机位数量 |

当前默认配置为：

```python
num_recovery_channels = 1
num_launch_channels = 1
num_fuel_servers = 20
num_arm_vehicles = 10
num_ammo_transport_vehicles = 10
num_lower_weapon_lifts = 6
num_upper_weapon_lifts = 4
num_personnel = 50
num_parking_spots = 45
```

### 3.1.3 波次层状态

波次层状态定义为：

$$
s_e^{wave}
=
\left(
k_e,\,
G_e^{launch},\,
G_e^{recovery},\,
\tau_e^{next}
\right)
$$

其中：

- $$k_e$$：当前波次编号；
- $$G_e^{launch}$$：当前放飞组，取 A 或 B；
- $$G_e^{recovery}$$：当前回收组，取 A、B 或 None；
- $$\tau_e^{next}$$：距离下一波次开始的时间。

该部分状态对强化学习很关键。若不提供波次信息，智能体无法判断某架飞机是否属于当前放飞组，也无法判断错过窗口的风险。

## 3.2 停机位与转运时间

当前模型保留停机位编号，但将甲板拓扑简化为一个挂弹车转运时间。

停机位转运时间为：

$$
t_i^{spot}
=
t_{base}
+
\text{layer}(z_i) \cdot t_{step}
$$

其中：

$$
t_{base}=2,\qquad t_{step}=1
$$

当前采用“每层固定 2 个停机位”的简化拓扑：

```text
spot 0: 2 min
spot 1,2: 3 min
spot 3,4: 4 min
spot 5,6: 5 min
...
```

该转运时间只用于挂弹车转运，不影响回收滑行、停放或放飞滑行。

## 3.3 候选集合与 Action Mask

### 3.3.1 待回收集合

飞机可回收，当且仅当：

- 属于当前回收组；
- 已经在空中；
- 标记为等待回收；
- 回收状态为未回收。

$$
I_e^{(r)}
=
\left\{
i
\mid
g_i = G_e^{recovery},
h_i = 1,
c_i = 1,
y_i^{(r)} = 0
\right\}
$$

### 3.3.2 待加油集合

飞机可加油，当且仅当：

- 不在空中；
- 已停放完成；
- 已完成回收；
- 尚未开始加油。

$$
I_e^{(f)}
=
\left\{
i
\mid
h_i = 0,
u_i = 2,
y_i^{(r)} = 2,
y_i^{(f)} = 0
\right\}
$$

### 3.3.3 待挂弹集合

飞机可启动挂弹流程，当且仅当：

- 不在空中；
- 已停放完成；
- 已完成回收；
- 尚未开始挂弹。

$$
I_e^{(m)}
=
\left\{
i
\mid
h_i = 0,
u_i = 2,
y_i^{(r)} = 2,
y_i^{(m)} = 0
\right\}
$$

### 3.3.4 待放飞集合

飞机可放飞，当且仅当：

- 属于当前放飞组；
- 不在空中；
- 已停放；
- 加油完成；
- 挂弹完成；
- 放飞状态为等待放飞。

$$
I_e^{(l)}
=
\left\{
i
\mid
g_i = G_e^{launch},
h_i = 0,
u_i = 2,
y_i^{(f)} = 2,
y_i^{(m)} = 2,
y_i^{(l)} = 1
\right\}
$$

### 3.3.5 高层 Action Mask

高层动作可行集合为：

$$
\mathcal{A}^H(s_e)
=
\{R \mid |I_e^{(r)}|>0,\ R_e>0\}
\cup
\{F \mid |I_e^{(f)}|>0,\ F_e>0,\ P_e \ge r_f\}
$$

$$
\cup
\{M \mid |I_e^{(m)}|>0,\ A_e>0,\ D_e>0,\ P_e \ge r_m\}
\cup
\{L \mid |I_e^{(l)}|>0,\ L_e>0\}
$$

其中：

- $$r_f$$：单架飞机加油所需保障人员；
- $$r_m$$：单架飞机挂弹所需保障人员。

注意：挂弹动作 M 只负责启动第一阶段，即弹药提取与下层升降机运输。后续从装配区到甲板挂弹阶段由环境在资源可用时自动尝试启动。

## 3.4 挂弹过程建模

当前挂弹不是单阶段任务，而是两阶段流程。

### 阶段 1：弹药提取与下层升降机运输

启动 M 动作后，占用：

- 弹药转运车；
- 下层武器升降机；
- 保障人员。

持续时间：

$$
p_i^{stage1}
=
p_i^{extract}
+
p_i^{lower}
$$

其中：

$$
p_i^{extract} \sim U(5,10)
$$

$$
p_i^{lower} \sim \mathcal{N}(3,\sigma_{lower}^2)
$$

代码中默认：

```python
lower_lift_time_mean = 3.0
lower_lift_time_std = 0.5
```

阶段 1 完成后，弹药转运车和下层升降机释放，飞机进入 `arm_stage = 2`，等待甲板挂弹资源。

### 阶段 2：上层升降机、甲板转运与逐弹挂载

当飞机处于 `arm_stage = 2`，且挂弹车与上层升降机可用时，环境自动启动阶段 2。

阶段 2 占用：

- 挂弹车；
- 上层武器升降机。

持续时间：

$$
p_i^{stage2}
=
p_i^{upper}
+
t_i^{spot}
+
\sum_{j=1}^{q_i} p_{ij}^{arm}
$$

其中：

$$
p_i^{upper} \sim \mathcal{N}(3,\sigma_{upper}^2)
$$

$$
p_{ij}^{arm} \sim \mathcal{N}(5, 2)
$$

代码中默认：

```python
upper_lift_time_mean = 3.0
upper_lift_time_std = 0.5
arm_unit_time_mean = 5.0
arm_unit_time_variance = 2.0
```

阶段 2 完成后：

```text
arm_status = 2
arm_stage = 4
```

## 3.5 事件触发机制

环境维护当前时间 $$t$$ 和未来事件队列。

当前事件类型包括：

| 事件 | 含义 |
|---|---|
| `wave_start` | 新波次开始 |
| `recover_done` | 回收完成 |
| `park_done` | 停放完成 |
| `fuel_done` | 加油完成 |
| `ammo_to_assembly_done` | 弹药到达装配区 |
| `arm_done` | 挂弹完成 |
| `launch_done` | 放飞完成 |

每次 `step(action)` 的逻辑为：

1. 根据当前状态生成 action mask；
2. 若存在可行动作，则执行智能体给出的动作；
3. 若动作合法，则启动对应任务并生成未来事件；
4. 若当前无可行动作，则时间推进到最近事件；
5. 处理该时刻所有事件；
6. 更新资源、飞机状态、等待时间和剩余时间；
7. 返回新状态、奖励、终止标志和 `info`。

## 3.6 Reward

由于当前目标是固定时长内提高出动架次并减少 missed sorties，奖励函数应围绕 sortie 和 miss 设计。

推荐形式为：

$$
r_e =
\beta_l \Delta N_e^{launch}
-
\beta_{miss} \Delta N_e^{miss}
-
\alpha \Delta t
-
\eta \mathbf{1}_{idle}
$$

其中：

- $$\Delta N_e^{launch}$$：该步完成放飞数量；
- $$\Delta N_e^{miss}$$：该步新增 missed sortie 数量；
- $$\Delta t$$：时间推进量；
- $$\mathbf{1}_{idle}$$：存在可行动作时选择空等待；
- $$\beta_l > 0$$；
- $$\beta_{miss} > 0$$；
- $$\alpha \ge 0$$。

当前代码中的奖励仍包含阶段完成 shaping：

```text
reward =
    - alpha * delta_t
    + beta_recovery * recovery_done
    + beta_fuel * fuel_done
    + beta_arm * arm_done
    + beta_launch * launch_done
```

该奖励可用于调试和训练，但最终评价应优先使用：

```text
total_sorties_completed
total_missed_sorties
```

如果后续用于正式强化学习训练，建议将 missed sortie 惩罚显式加入奖励函数。

## 3.7 终止条件

早期单轮模型的终止条件是所有飞机完成放飞。

当前多波次模型的终止条件为：

$$
t \ge T_{\max}
$$

其中 $$T_{\max}$$ 为固定仿真时长，即 `simulation_duration`。

## 4. 当前环境接口

当前环境接口类似 Gym，但无第三方 Gym 依赖：

```python
class CarrierAircraftSchedulingEnv:
    def reset(self, seed=None):
        pass

    def step(self, action):
        pass

    def get_state(self):
        pass

    def get_action_mask(self, high_level_action=None):
        pass

    def get_aircraft_timing_records(self):
        pass

    def get_wave_records(self):
        pass

    def get_missed_sortie_records(self):
        pass

    def get_evaluation_metrics(self):
        pass
```

统一求解入口为：

```bash
python solve.py --solver heuristic --num-aircraft 40 --wave-interval 60 --simulation-duration 720 --runs 10
```

当前可用 solver：

```text
random
heuristic
sampled
```

## 5. 配置参数

主要配置位于 `env/config.py`。

默认关键参数如下：

```python
DEFAULT_CONFIG = {
    "num_aircraft": 40,
    "group_size": 20,
    "num_parking_spots": 45,

    "wave_interval": 120.0,
    "simulation_duration": 720.0,

    "num_recovery_channels": 1,
    "num_launch_channels": 1,
    "num_fuel_servers": 20,
    "num_arm_vehicles": 10,
    "num_ammo_transport_vehicles": 10,
    "num_lower_weapon_lifts": 6,
    "num_upper_weapon_lifts": 4,
    "num_personnel": 50,

    "recovery_time": 1.0,
    "launch_time": 1.0,

    "fuel_time_mean": 20.0,
    "fuel_time_std": 3.0,

    "arm_unit_time_mean": 5.0,
    "arm_unit_time_variance": 2.0,

    "ammo_extract_time_min": 5.0,
    "ammo_extract_time_max": 10.0,
    "lower_lift_time_mean": 3.0,
    "lower_lift_time_std": 0.5,
    "upper_lift_time_mean": 3.0,
    "upper_lift_time_std": 0.5,

    "arm_quantity_values": [1, 2, 3, 4],
    "arm_quantity_probs": [0.3, 0.4, 0.2, 0.1],
}
```

## 6. 建模注意事项

当前模型相比完整舰面作业仍有简化：

1. 停机位只影响挂弹车转运时间，不影响回收和放飞滑行；
2. 回收后停放为即时事件，没有牵引车和滑行路径约束；
3. 没有显式建模甲板路径冲突；
4. 没有弹药型号、库存和装配区容量；
5. 人员占用仍是聚合式处理，尚未严格按阶段释放；
6. 回收和放飞均为单通道固定时间；
7. 奖励函数与最终 sortie/miss 指标仍可进一步对齐。

若后续继续提高模型可信度，建议优先补充：

- 装配区容量；
- 挂弹车路径和队列；
- 停机位对放飞前滑行的影响；
- 回收后滑行入位时间；
- 人员按阶段占用与释放；
- 显式 missed sortie 惩罚奖励。
